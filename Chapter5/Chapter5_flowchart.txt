1. number baseballgame
    # 순서도 그리기
        1. 숫자 네 개를 뽑는다.
        2. 답을 제출한다.
        3. 답이 네 개인지 검사한다.
        4. 홈런인지 검사한다.
        5. 홈런이 아닐 경우, 스트라이크, 볼을 계산한다.
        6. 2-5과정을 10번 반복한다(반복중 홈런일 경우 7번으로 간다).
        7. 최종 결과를 표시한다.

2. number baseballgame self
    # 순서도 그리기
        1. 숫자 네 개를 뽑는다.
        2. 답을 제출한다.
        3. 답이 네 개인지 검사한다.
        4. 홈런인지 검사한다.
        5. 홈런이 아닐 경우, 스트라이크, 볼, 아웃을 계산한다.
        6. 2-5과정을 10번 반복한다(반복중 홈런일 경우 7번으로 간다 +아웃이 3이상일 경우도 포함).
        7. 최종 결과를 표시한다.

